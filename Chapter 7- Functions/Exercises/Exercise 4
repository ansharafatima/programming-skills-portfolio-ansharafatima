#Modify the make_shirt() function so that shirts are large by default with a message that reads I love Python. Make a large shirt and a

#medium shirt with the default message, and a shirt of any size with a different message
def make_shirt(size="small", message="I love Python"):
    print(f"Making a shirt of size {size} with the message: '{message}'.")

# function calling to make a large shirt with the default message.
make_shirt()

#function calling to make a medium shirt with the default message.
make_shirt(size="medium")

#function calling to make a shirt of any size with a different message.
make_shirt(size="xl", message="hehe!")
