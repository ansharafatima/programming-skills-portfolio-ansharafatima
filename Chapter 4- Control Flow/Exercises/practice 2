#2. Write an if-else statement that assigns O to the variable b if the variable a is less than 10.
#Otherwise, it should assign 99 to the variable b.

a = 7  # You can assign any value to 'a' for testing

if a < 10:
    b = 0
else:
    b = 99

print("a =", a)
print("b =", b)
